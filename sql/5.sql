DELIMITER $$
CREATE PROCEDURE days()
BEGIN
  DECLARE i BIGINT DEFAULT 0;
  WHILE i < 4095518177000 DO
    INSERT INTO days (timestamp) VALUES (i);
    SET i = i + 86400000;
  END WHILE;
END$$
DELIMITER ;